# Zettelkasten (dev)

One idea per note, and the note earns its keep by what it links to. ^atomic

A bare [[Zettelkasten]] written *here* resolves to this note's own neighbourhood
before it looks anywhere else — but this folder has no other `Zettelkasten`, so
it walks outward and finds the root one.

Deeper still: [[deep/Zettelkasten]].

Some maths, because notes have maths in them: $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$
