# v1.2 spec

This note exists to prove a small point: its filename contains a dot, and
"everything after the last dot" is not a file extension.

A tool that assumes otherwise indexes this note as `v1`, and every
[[v1.2 spec|link to it]] silently dies.
